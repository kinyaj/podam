## Customize the test machine
machine:
  # Version of jdk
  java:
    version:
      oraclejdk8
      oraclejdk7


dependencies:
  override:
    - mvn dependency:resolve


test:
  override:
    - mvn clean verify

general:
  branches:
    only:
      - develop
