## Customize the test machine
machine:
  # Version of jdk
  java:
    version:
      oraclejdk8

test:
  override:
    - mvn integration-test

general:
  branches:
    only:
      - develop

dependencies:
  mvn:
    dependency:resolve -DexcludeGroupIds=com.github.github -V dependency:go-offline
