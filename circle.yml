## Customize the test machine
machine:
  # Version of jdk
  java:
    version:
      oraclejdk8

test:
  override:
    - mvn integration-test

general:
  branches:
    only:
      - develop

dependencies:
  pre:
    - mvn clean verify
